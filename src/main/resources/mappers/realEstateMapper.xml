<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.uilangage.spring.test.mybatis.repository.RealEstateRepository">
 	<select id="selectRealEstate" parameterType="int"
 	resultType="com.uilangage.spring.test.mybatis.domain.RealEstate">
 		SELECT
 			`id`
 			, `realtorId`
 			, `address`
 			, `area`
 			, `type`
 			, `price`
 			, `rentPrice`
 			, `createdAt`
 			, `updatedAt`
 		FROM	
 			`real_estate`
 		WHERE
 			`id` = #{id}	
 	</select>
 
 	<select id="rentPriceEstate" parameterType="int"
 	resultType="com.uilangage.spring.test.mybatis.domain.RealEstate">
 		SELECT
 			`id`
 			, `realtorId`
 			, `address`
 			, `area`
 			, `type`
 			, `price`
 			, `rentPrice`
 			, `createdAt`
 			, `updatedAt`
 		FROM	
 			`real_estate`
 		WHERE
 			`type` = '월세'
 			AND <![CDATA[`rentPrice` < #{rentPrice}]]>
 			
 	</select>
 
 	<select id="selectEstateAreaPrice" parameterType="int">
 		<![CDATA[
 		SELECT
 			`id`
 			, `realtorId`
 			, `address`
 			, `area`
 			, `type`
 			, `price`
 			, `rentPrice`
 			, `createdAt`
 			, `updatedAt`
 		FROM	
 			`real_estate`
 		WHERE
 			`area` > #{area}
 		AND	
 			`price` < #{price}
 		ORDER BY
 			`createdAt`
		DESC	
 			]]>
 	
 	</select>
 
 
 </mapper>